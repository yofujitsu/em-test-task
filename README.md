# 🚀 Backend-система управления банковскими картами

## 💡 Описание
Разработана **backend-система для управления банковскими картами**.  
Система реализована на Java с использованием Spring Boot и поддерживает ролевую модель доступа к API (`ADMIN`, `USER`).

Функционал включает:
- ✅ Создание и управление картами
- ✅ Денежные переводы между картами
- ✅ Просмотр карт с фильтрацией и пагинацией

## ⚙️ Технологии
- **Java 17+**
- **Spring Boot** (Data JPA, Security, Validation)
- **PostgreSQL**
- **Liquibase** (миграции)
- **Docker, Docker Compose**
- **JWT (аутентификация и авторизация)**
- **JUnit 5 + Mockito** (тестирование)
- **OpenAPI/Swagger** (документация)

## 🔐 Реализованные возможности
- Ролевой доступ к API (`ADMIN`, `USER`)
- Глобальный обработчик ошибок с кастомными исключениями и типизированными респонсами
- Внедрение транзакций в бизнес-логику
- Спецификация для поиска карт по полям
- Маскирование номеров карт (`**** **** **** 1234`)
- CRUD-операции с картами
- Переводы между картами
- Управление картами и пользователями (для `ADMIN`)

## 🧪 Тестирование
- Unit-тесты для слоя контроллеров и сервисов
- Использование JUnit 5 и Mockito

## 📦 Развёртывание
В проекте реализованы:
- **Dockerfile** для сборки Java-приложения
- **docker-compose.yml** для запуска инфраструктуры (приложение + PostgreSQL)

## 📑 Документация
Документация API в формате **OpenAPI** находится в папке [`docs`](./docs).

После запуска доступна **Swagger UI**:  
👉 [http://localhost:8080/swagger-ui.html](http://localhost:8080/swagger-ui.html)

## 🚀 Запуск
```bash
# Клонировать репозиторий
git clone <repo-url>
cd <project-folder>

# Запустить проект
docker-compose up -d